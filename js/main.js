$(window).on('load', function () {
  $('#header').vide('./video/cover', {
    bgColor: '#011c15'
  });
});
$(window).on('load', function () {
  $('#footer').vide('./video/cover', {
    bgColor: '#011c15'
  });
});